spring.application.name=auditservice
server.port=${SERVER_PORT:8086}

# Default works for host runs; compose overrides with the internal listener (kafka:29092).
spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
spring.kafka.consumer.group-id=audit-group
spring.kafka.consumer.auto-offset-reset=earliest

# Consume the raw JSON value as a String (no Jackson dependency needed).
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer
